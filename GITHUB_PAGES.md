# GitHub Pages

1. Create a GitHub repository and upload the contents of this folder.
2. Open repository settings: `Settings -> Pages`.
3. Set `Source` to `GitHub Actions`.
4. Push to the `main` branch.
5. Open `Actions -> Deploy GitHub Pages` and wait for the workflow to finish.
6. Open the published URL. It will redirect to `quiz/index.html`.

On iPhone:

1. Open the GitHub Pages URL in Safari.
2. Open both `Тести` and `Відкрита форма` once while online.
3. Tap `Share -> Add to Home Screen`.
4. After that, the app should open from the home screen and keep the cached files available offline.
